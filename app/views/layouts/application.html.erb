<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Job Application Tracker" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
  <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <%# Includes all stylesheet files in app/assets/stylesheets %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
<nav class="bg-white border-b border-gray-200 shadow-sm fixed w-full top-0 left-0 z-40">
  <div class="container mx-auto px-5 py-3 flex justify-between items-center">
    <!-- Left side: Brand / Home -->
    <div class="flex items-center space-x-6">
      <%= link_to "Home", root_path, class: "text-lg font-semibold text-gray-800 hover:text-gray-900" %>
    </div>

    <!-- Right side: Auth links -->
    <div class="flex items-center space-x-4">
      <% if authenticated? %>
        <%= button_to "Sign out",
                      session_path,
                      method: :delete,
                      class: "bg-red-500 text-white px-4 py-2 rounded-lg shadow-sm hover:bg-red-600 transition-colors" %>
      <% else %>
        <%= link_to "Sign up",
                    sign_up_path,
                    class: "bg-blue-500 text-white px-4 py-2 rounded-lg shadow-sm hover:bg-blue-600 transition-colors" %>
        <%= link_to "Sign in",
                    new_session_path,
                    class: "text-gray-600 hover:text-gray-900 font-medium transition-colors" %>
      <% end %>
    </div>
  </div>
</nav>


<% if notice.present? %>
  <div
    id="notice"
    data-controller="flash"
    data-flash-timeout-value="10000"
    class="fixed top-4 left-4 z-50 bg-green-50 text-green-600 px-4 py-2 rounded-lg shadow-lg font-medium transition-opacity"
  >
    <%= notice %>
  </div>
<% end %>

<% if alert.present? %>
  <div
    id="alert"
    data-controller="flash"
    data-flash-timeout-value="10000"
    class="fixed top-4 left-4 z-50 bg-red-50 text-red-600 px-4 py-2 rounded-lg shadow-lg font-medium transition-opacity"
  >
    <%= alert %>
  </div>
<% end %>
<main class="container mx-auto mt-24 px-5 flex">
  <%= yield %>
</main>
</body>
</html>
